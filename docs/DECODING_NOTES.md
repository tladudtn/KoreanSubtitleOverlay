# 개발 참고 문서 (세션 간 컨텍스트용)

이 문서는 다른 세션(다른 대화)에서 이 프로젝트를 다시 열었을 때, 그동안의
조사/결정 과정을 처음부터 다시 하지 않도록 남기는 기술 참고 자료다.
사용자용 요약은 [README.md](../README.md), 비개발자용 안내는
[가이드.md](가이드.md) 참고. 이 문서는 자모 디코딩 로직의 "왜 이렇게
매핑했는지", "왜 이렇게 고쳤는지"의 근거를 전부 담는다 - 후킹/렌더링
구조 자체는 [ARCHITECTURE.md](ARCHITECTURE.md), fonts.txd HD 병합은
[HD_FONT_MERGE.md](HD_FONT_MERGE.md) 참고.

## 프로젝트 구조

```
C:\Users\SYS\Documents\GTA-SA-Dev\
├── KoreanSubtitleOverlay\   (이 프로젝트, git 저장소)
│   ├── src\dllmain.cpp      실제 플러그인 소스 (디코더 + 오버레이 렌더링 본체)
│   ├── src\sa_messages.h    게임 구조체/주소 (plugin-sdk 참조)
│   ├── tools\gxt_scan.cpp   오프라인 전수 검증 도구 (아래 참고)
│   └── docs\                문서 모음 (이 파일, ARCHITECTURE.md, HD_FONT_MERGE.md,
│                             가이드.md, screenshots\)
├── imgui\                   (사이드로드, 프로젝트에 포함 안 됨)
├── minhook\                 (사이드로드, 프로젝트에 포함 안 됨)
├── FontExtract\             fonts.txd 글리프 추출 + merge_fonts 병합 도구 (별도 프로젝트,
│                             자세한 내용은 HD_FONT_MERGE.md)
└── txdedit\                 서드파티 오픈소스 TXD 편집기 clone, libtxd가 위 도구들의 실제 구현체
```

게임 설치 경로: `C:\Program Files (x86)\Steam\steamapps\common\Grand Theft Auto San Andreas`
- 배포 위치: `modloader\_BASIC\KoreanSubtitleOverlay\KoreanSubtitleOverlay.asi`
- 원본 한글패치 에셋(참조용): `modloader\_BASIC\Korean_Patch\{american.gxt, fonts.dat, fonts.txd}`

## 문제 배경

이 한글패치의 `fonts.txd`는 진짜 텍스트 인코딩이 아니라 "자모조합형" 폰트다.
GXT 대사 문자열의 바이트가 각각 완성형 글자가 아니라 초성/중성/종성
"조각" 하나씩을 가리키고, 게임 렌더러가 2~4개를 겹쳐 그려서 하나의
한글 음절처럼 보이게 만드는 방식(옛날 조합형 콘솔 폰트와 동일한 트릭).
네이티브 렌더러가 이미 이 결합 방식을 잘못 처리해서 자막이 깨져 보인다.

이 플러그인은 `CMessages::BIGMessages`/`BriefMessages`에서 원본 바이트를
직접 읽어서 우리가 자체적으로 재조합 -> 유니코드 한글 음절로 변환한 뒤
ImGui로 그려서 네이티브 드로우를 대체한다.

## 바이트 레이아웃

- `0x00–0x7F`: 표준 ASCII 그대로 통과
- `0x80–0x92`: 초성(19개, ㄱ~ㅎ 표준 순서)
- `0x93–0x9C`: 중성(10개 기본 모음: A YA EO YEO O YO U YU EU I)
- `0x60`, `0x5D`: "종성 없음" 종결 바이트 (두 종류 다 씀, 이유 불명이나 상호
  교환 가능하게 처리)
- `0x9D`, `0x9E`, `0x9F`, `0xA1`–`0xAF`: 종성(받침) 바이트 - 순서가 초성/중성처럼
  단순 연속이 아니라서 실제 확인된 것만 표로 정리(아래)
- `0x5C`, `0x9C`: 겹모음 만들 때 쓰는 "+ㅣ" 보정 바이트(두 값이 같은 역할)

## 확정된 종성(받침) 매핑

전부 `american.gxt` 전수 스캔(아래 도구) + 다수 문장에서 자연스러운 실제
단어로 확인됨. `0xAE`만 텍스트 근거가 하나뿐이라 FontExtract 글리프
비교로 추가 검증함(아래 "글리프 검증" 참고).

| 바이트 | 자모 | 유니코드 | 근거 단어 |
|---|---|---|---|
| 0x3C | ㅌ | U+314C | 같이 (2회, ASCII `<` 슬롯 재사용) |
| 0x9D | ㄱ | U+3131 | 가속하려면, 죽기엔 (가장 흔한 종성, 압도적 빈도) |
| 0x9E | ㄲ | U+3132 | 밖에 ×3, 낚이던데 |
| 0x9F | ㄴ | U+3134 | 만 |
| 0xA1 | ㄶ | U+3136 | 않아, 않습니다, 있잖아 (**초기에 U+3135로 잘못 매핑했었음 - 이건 실제로는 ㄶ이 아니라 ㄵ의 코드포인트임(한 글자 오타). 수정됨** - 잖아/귀찮아 등 ㄶ받침 음절이 전부 ㄵ받침으로 잘못 렌더링되던 원인) |
| 0xA2 | ㄷ | U+3137 | 받고, 싣는, 얻어, 믿는 |
| 0xA3 | ㄹ | U+3139 | 를, 살 |
| 0xA4 | ㄺ | U+313A | 닭대가리, 닭살 |
| 0xA5 | ㄻ | U+313E | 옮기나 |
| 0xA6 | ㄼ | U+313B | 밟기 |
| 0xA7 | ㅀ | U+3140 | 잃기, 잃었습니다, 싫으니 |
| 0xA8 | ㅁ | U+3141 | 이놈아, 임마, 좀 (**초기에 ㅇ으로 잘못 매핑했었음, 수정됨**) |
| 0xA9 | ㅂ | U+3142 | 십, 습 |
| 0xAA | ㅄ | U+3144 | 없다, 값 |
| 0xAB | ㅅ | U+3145 | 그것도, 것을, 거짓말쟁이, 멋쟁이 |
| 0xAC | ㅆ | U+3146 | 있 |
| 0xAD | ㅇ | U+3147 | 상황 (두 음절 다 이 종성 공유) |
| 0xAE | ㅈ | U+3148 | "좆같이도" 슬랭 + FontExtract 글리프 비교 (0xAB와 형태 비교, 위 획 하나 추가됨) |
| 0xAF | ㅊ | U+314A | 몇 (9회 이상), 쫓는 |
| 0x3E | ㅍ | U+314D | 무릎 (`86 99 5D 85 9B 3E 5D`) - **`finalJamoForByte()`의 `switch`문 안에 case를 넣었더니 왜인지 인게임에서 계속 미매핑으로 나옴** (재현은 확실히 됐으나 원인 미규명, MSVC 코드생성 관련 의심). 근본 원인을 못 찾아서 `switch`문에 진입하기 *전에* 이 바이트만 먼저 걸러내는 `if (b == 0x3E) return 0x314D;`를 앞에 추가해서 우회함 - 코드 안 주석 참고 |

## 겹모음(compound vowel) 결합 규칙

두 가지 방식:
1. **두 모음 바이트 겹치기**: `O(0x97)+A(0x93)=WA`, `U(0x99)+EO(0x95)=WEO`
2. **"+ㅣ" 보정 바이트**: `A+i=AE`, `EO+i=E`, `EU+i=YI`, `O+i=OE`, `U+i=WI`,
   `YEO+i=YE`(미셸 확인), `YA+i=YAE`(얘기: `80 9B 5D 86 93 9F 60 20 8B 94 5C 60 80 9C 60`
   에서 확인 - 처음엔 이 조합이 없어서 "얘기"가 "야"+스트레이 문자로 깨졌었음)
   - 이때 "+i" 바이트는 `0x5C` **또는** `0x9C`(순수
   "이" 중성 바이트 재사용, "두려움의"/"뒤에서"로 확인, 발견 당시 가장 큰
   버그 원인이었음 - "의" 조사가 게임에서 워낙 흔해서)
3. **3중 겹침(트리플 스택)**: 이미 결합된 WA/WEO 뒤에 `0x5C`가 한 번 더
   오면 WA→WAE, WEO→WE로 업그레이드됨 ("됐어" = 됐 = ㄷ+WAE+ㅆ로 확인)

## 검증 방법론

### 1) 오프라인 GXT 전수 스캔 (`tools/gxt_scan.cpp`)

`dllmain.cpp`와 완전히 동일한 디코드 로직을 복제해서, 실제 게임을 켜지
않고 `american.gxt` 파일 자체를 파싱해 모든 대사 문자열(127개 테이블,
총 1775개 한글 인코딩 문자열)에 대해 디코딩을 돌려보는 진단 도구.
미매핑 바이트를 만나면 hex + 디코딩 결과를 리포트에 남긴다.

**GXT 파일 구조** (SA 표준): `TABL`(테이블명+오프셋 목록) → 각 테이블마다
`TKEY`(8바이트 엔트리: offset+hash) → `TDAT`(실제 null-terminated 문자열
블롭). TKEY 영역을 문자열로 착각해서 스캔하면 노이즈가 대량 발생하니
반드시 TDAT 영역만 스캔해야 함 (초기 버전에서 이 실수로 128개의 가짜
미매핑 바이트가 나왔었음).

빌드/실행법:
```
call "C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools\VC\Auxiliary\Build\vcvars64.bat"
cl.exe /EHsc /nologo /Fe:gxt_scan.exe tools\gxt_scan.cpp /link user32.lib

gxt_scan.exe "<게임경로>\modloader\_BASIC\Korean_Patch\american.gxt" report.txt
```
마지막 줄 요약에 `distinct unmapped byte values`가 0이면 전수 검증 통과.
**주의**: `finalJamoForByte`/`combineMedial` 테이블은 `dllmain.cpp`와
수동으로 동기화해야 함(자동 공유 안 됨) - `dllmain.cpp`를 고치면 이
파일도 같이 고칠 것.

### 2) FontExtract 글리프 직접 대조

`../FontExtract`가 `fonts.txd`의 각 32x32 글리프 셀을 `cell_XX.png`로
개별 추출해준다 (`output/1_font1_cells/cell_XX.png`, XX=hex 바이트).
**`font1`이 실제 자모 폰트**(font2는 WASTED/BUSTED/MISSION PASSED 고딕체 +
지역명/차량명용 일반 서체 - 이후 세션에서 확인됨, [HD_FONT_MERGE.md](HD_FONT_MERGE.md)
참고). 이미지를
직접 열어서(Read 툴로 - Claude는 이미지를 볼 수 있음) 이미 확정된
바이트의 글리프 모양과 비교하면 텍스트 근거가 약한 바이트도 검증 가능.
격자 전체 이미지(`_grid.png`, 빨간 격자선 포함)를 크롭해서 여러 바이트를
한 번에 비교하면 셀 경계 정렬도 같이 확인된다(받침 글리프는 셀 하단에
배치되는 게 정상이라 "잘린 것처럼" 보이는 건 버그가 아님).

### 3) 인게임 로그 + ffmpeg 프레임 단위 영상 대조

디코딩이 아니라 "언제, 어떤 순서로 그려지는가" 계열 버그(자막 전환 시
겹침/깜빡임 등)는 바이트 매핑 검증만으로는 못 잡는다. 이런 건 실제
플레이 중 타이밍이 문제라서, 이번 세션에서 쓴 방법은:

1. `dllmain.cpp`에 상태 전환이 있을 때만 찍는 디버그 로그를 추가한다
   (매 프레임 로그하면 로그가 너무 커지므로, "선택된 슬롯이 바뀔 때만",
   "네이티브 억제 on/off가 바뀔 때만" 식으로 트리거를 걸었다 - 아래
   `LogSubtitleSelectionIfChanged`, `hkDisplay`의 `[display-debug]` 로그 참고).
2. 사용자가 문제되는 구간을 화면 녹화(Xbox Game Bar 등, `C:\Users\SYS\Videos`)한다.
3. `ffmpeg`(winget으로 설치: `winget install --id Gyan.FFmpeg`)로 영상에서
   자막이 나오는 화면 하단 영역만 crop해서 **60fps 그대로(다운샘플링 없이)**
   프레임 이미지로 추출한다 (`ffmpeg -i in.mp4 -vf "crop=W:H:X:Y" f_%05d.jpg`).
4. 프레임 수가 많아서(51초 영상 기준 3097장) 한 장씩 보는 대신, ffmpeg의
   `tile` 필터로 여러 프레임을 한 장의 콘택트시트 이미지로 묶어서
   (`scale=작게,tile=5x50` 식) 한 번에 훑어본다. 이러면 Read 툴 호출
   몇 번으로 전체 영상을 다 확인할 수 있다.
5. 로그의 타임스탬프(`GetTickCount()` 기반)와 영상 속 대사 내용을 나란히
   대조해서, "그 순간 실제로 무슨 일이 있었는지"를 재구성한다.

**주의**: 콘택트시트를 너무 작게 스케일하면(예: 384x30px 셀) 사람 눈으로
훑어볼 때 "겹쳐 보이는" 것 같은 명백한 이상은 잘 보여도, "문장이 한 글자
덜 나온" 것처럼 텍스트 내용을 한 글자 한 글자 비교해야 알 수 있는 미묘한
차이는 놓치기 쉽다 - 실제로 이번 세션에서 그렇게 한 번 놓친 사례가 있었고,
사용자가 스크린샷으로 직접 짚어준 뒤에야 발견했다. 의심되는 특정 구간은
스케일을 낮추지 말고 개별 프레임을 확대해서 봐야 한다.

## 오버레이 렌더링 로직 (자막 선택 / 네이티브 억제 / 전환 버그)

디코딩과는 별개로, "여러 활성 메시지 중 뭘 화면에 그릴지"와 "네이티브
자막 그리기를 언제 꺼둘지"를 다루는 로직도 여러 차례 버그가 있었다.

### 자막 선택: `PickCurrentMessage()`

`BIGMessages`(스타일 7종) + `BriefMessages`(슬롯 8개), 총 15개 슬롯을 매
프레임 훑어서 그 중 실제로 활성(텍스트 있음 + 한글 바이트 포함)인 것들
중 `tMessage::m_dwStartTime`(그 대사가 시작된 시각)이 가장 큰, 즉 가장
최근에 시작된 것 하나만 골라서 그린다.

처음엔 슬롯별로 "마지막으로 본 텍스트"를 따로 추적하다가 바뀐 슬롯을
그때그때 히스토리에 쌓는 방식이었는데, 게임이 옛날 슬롯을 새 슬롯보다
늦게 정리하는 경우가 있어서 옛 대사가 다시 노출될 여지가 있었다.
`m_dwStartTime` 비교 방식으로 바꿔서, 어느 슬롯이 갱신을 늦게 하든 상관없이
항상 "실제로 가장 최근에 시작된" 대사만 골라지도록 했다.

### 네이티브 억제 타이밍: `hkDisplay`

`CMessages::Display()`(네이티브, 깨진 글자로 그리는 원본 함수)를 후킹해서
한글이 활성일 때는 호출을 건너뛰는데, 처음엔 "지금 이 순간 한글 텍스트가
있는가"만 보고 즉시 on/off를 했다. 문제는 **네이티브 쪽이 `m_pText`가 이미
비어도 자기 나름의 타이머(`tMessage::m_dwTime`, 그 대사의 선언된 표시
시간)로 한동안 더 그 줄을 그리고 있다는 것** - 그래서 우리가 "비었다"고
판단한 순간에 네이티브를 풀어주면, 네이티브가 그 짧은 틈에 이전 줄을
한 프레임 깨진 글자로 그려버려서 사용자 눈엔 "이전 대사가 겹쳐 보인다"로
보였다.

고친 방식: 한글 메시지가 활성이면 그 메시지의 `m_dwTime`(ms)만큼, 실제
시계(`GetTickCount()`) 기준으로 억제를 유지한다. 그런데 실제 로그로
확인해보니 어떤 전환은 이 시간이 끝나는 시점과 **6ms 차이**로 아슬아슬하게
걸렸다 - 스크립트로 고정된(정해진 순서의) 미션 대사라 재현할 때마다 같은
지점에서 같은 경계에 걸리는 바람에 "항상 똑같이" 겹치는 것처럼 보였다.
그래서 500ms 여유(`kSuppressSafetyMarginMs`)를 추가로 더 얹어서 이 경계
충돌 자체를 없앴다.

### ImGui 자동 리사이즈 지연으로 인한 문장 잘림

위 문제를 고친 뒤에도 "짧은 줄 다음에 긴 줄이 나올 때, 그 첫 프레임엔
문장이 잘려 보였다가 다음 프레임에 완성되는" 증상이 남아 있었다. 이건
디코딩/네이티브 문제가 아니라 **ImGui 쪽 문제**였다: `ImGuiWindowFlags_
AlwaysAutoResize`는 그 프레임에 그려진 내용 크기를 반영해서 창을 넓히는
게 한 프레임 늦게 적용된다. 그래서 텍스트 자체는 처음부터 완성된 문장으로
정확히 디코딩됐는데도(로그에도 잘린 버전은 안 찍힘 - 완성된 문장만 한
번 로그됨), 화면엔 이전(좁은) 창 크기로 클리핑돼서 잘려 보였다.

고친 방식: 자동 리사이즈에 맡기지 않고, 매 프레임 텍스트 크기를 직접
측정해서 `ImGui::SetNextWindowSize()`로 그 프레임부터 바로 정확한 크기를
강제 지정하도록 바꿨다.

### ESC(일시정지) 메뉴에서 자막 숨기기

`CMenuManager::m_bMenuActive`(plugin-sdk 기준 `FrontEndMenuManager`
싱글턴 `0xBA6748` + 오프셋 `0x5C`, `VALIDATE_OFFSET` 매크로로 검증됨)가
true면 오버레이 그리기 자체를 건너뛴다. 메뉴를 닫으면 다음 프레임에
`PickCurrentMessage()`가 살아있는 상태를 다시 평가해서 자동으로 복귀한다.

## 세션 타임라인 요약

1. 겹모음 "와" 버그를 `combineMedial()`로 최초 수정
2. 실제 로그(`KoreanSubtitleOverlay.log`, 262줄) 수동 분석 -> "가<`이도",
   "사`화" 같은 스트레이 ASCII 버그 원인이 미매핑 종성이라는 걸 특정,
   0xA8을 ㅇ->ㅁ로 정정, 0x3C/0xAD 신규 확정
3. "됐어" 3중 겹모음 버그 -> `upgradeMedialWithI()` 추가
4. 게임을 직접 조작할 도구가 없어서(데스크톱 자동화/스크린샷 툴 없음),
   대신 `american.gxt` 전체를 오프라인으로 파싱하는 `gxt_scan.cpp`를
   작성해서 전수 검증하는 방식으로 전환 - 미션 하나 플레이하는 것보다
   훨씬 철저하게 검증됨
5. GXT 구조 파싱 실수(TKEY 영역 오염) 수정 후 재스캔 -> 0x9D=ㄱ,
   0x9E=ㄲ, 0xA1=ㄶ, 0xA2=ㄷ, 0xA4=ㄺ, 0xA5=ㄻ, 0xA6=ㄼ, 0xA7=ㅀ,
   0xAA=ㅄ, 0xAB=ㅅ, 0xAF=ㅊ 순차 확정, 0x9C "+i" 결합 발견(최대 버그
   원인), 미매핑 바이트 0개 달성
6. 0xAE(ㅈ)만 텍스트 근거가 하나뿐이라 FontExtract 글리프 비교로 추가
   확인 (0xAB=ㅅ과 형태 비교, 위 획 하나 차이)
7. 인게임 실제 렌더링 검증: 보더리스 윈도우 상태에서 GDI 화면 캡처
   (`System.Drawing.Graphics.CopyFromScreen`)로 실제 게임 화면을 찍고,
   저수준 키 입력(`user32.dll keybd_event`)으로 메뉴 탐색 + 이동을 수행해
   "BIG SMOKE" 세이브를 불러온 뒤 Ryder 미션 진입 대사("라이더를 만나러
   가십시오. 이 거리에 살고 있습니다.")가 ImGui 오버레이로 정상 렌더링되는
   걸 스크린샷으로 확인함.
8. 사용자가 "캐릭터 간 대사(Ryder/CJ 미션 인트로 대화)가 안 나온다"고
   지적 - 실제로는 게임 옵션의 "자막" 설정이 꺼져 있으면 해당 대사 자체가
   `CMessages` 큐에 올라오지도 않는다는 걸 확인. `PrefsShowSubtitles`
   (`0xBA678C`, GTAMods 위키 출처라 미검증)를 매 프레임 강제로 true로
   덮어써서, 자막 옵션과 무관하게 항상 큐가 채워지도록 함(`ForceSubtitlesPref`).
9. 사용자 요청으로 스타일 변경: 글자 1.5배(`kKoreanFontSize`), 실제 굵은
   글꼴 파일(`malgunbd.ttf`)로 교체, `AddOutlinedText()`로 수작업 텍스트
   윤곽선 추가, 배경 상자 제거(`ImGuiWindowFlags_NoBackground`).
10. 오탈자 수정("무름"→"무릎", 사실은 0x3E ㅍ 미매핑 버그였음, 위 표 참고)
    및 컷신 진입 시마다 "라이더"를 하드코딩으로 띄우던 남은 디버그 코드
    (`CheckCutsceneEntry`) 완전 삭제.
11. MISSION PASSED / BUSTED / WASTED 배너가 원래 영어인데도 우리 오버레이
    폰트로 다시 그려지고 있던 문제 발견 - `hkDisplay`가 무조건 네이티브를
    죽이고 있었기 때문. `ContainsHighByte()`/한글 포함 여부로 판단해서,
    한글이 없는(=순수 ASCII) 배너는 `ProcessSlot`/선택 로직에서 아예
    무시하고 네이티브 그리기를 그대로 통과시키도록 수정.
12. 한국어 문서화: 개발자용 `README.md` 전체 한글화, 비개발자용
    `docs/가이드.md` 신규 작성, 문서를 `docs/`로 재정리, README에 실제
    체포(busted) 상황 스크린샷 추가, 한글패치를 런타임 의존성으로 명시.
13. README에 TODO로 남겼던 두 가지 잔버그 조사/수정:
    - ESC 메뉴 진입 시 자막이 안 숨겨지던 문제 -> `CMenuManager::m_bMenuActive`
      확인 후 오버레이 그리기 스킵 (위 "오버레이 렌더링 로직" 섹션 참고).
    - 대사 전환 시 이전 줄이 겹쳐 보이는 문제 -> 처음엔 슬롯 선택 로직을
      의심해서 두 차례 다시 설계했지만 재현됨. 사용자가 직접 화면 녹화한
      영상과 진단 로그를 프레임 단위로 대조(위 "검증 방법론 3)" 참고)한
      끝에, 실제 원인이 두 개(네이티브 억제 타이밍 + ImGui 자동 리사이즈
      지연)라는 걸 확인하고 각각 수정. ffmpeg 프레임 단위 검증으로 완전히
      재현 안 되는 것까지 확인.
